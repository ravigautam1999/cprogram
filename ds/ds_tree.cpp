#include<iostream>
#include<bits/stdc++.h>

using namespace std;

int main() {
    long long int n;
    vector<vector<int>> v[n];
    vector<int> d;

    cout <<"enter Tree size: "<<endl;
    cin>>n;
    for(int i=0; i< n; i++){
        char c;
        int a;
        cout<<"if want to end the process:\n";
        int f=0;
        do{
            cin>>a;
            f+=1;
            
        }while(c);
       

        d.resize(0);
    }

    return 0;
}
